#include <ws2tcpip.h>
#include <iostream>

#pragma comment(lib, "ws2_32.lib")

// 클라이언트 통신 데이터 처리
// 메세지 수신(메세지를 저장할 용량 할당, 수신한 메세지 출력)
// 연결이 끊어졌을 경우 소켓을 닫음
void handle_client(SOCKET client_socket) {

}
// 메인 함수
int main() {
	//Winsock초기화 작업

	//소켓 생성

	//서버 주소 할당

	//연결 요청 대기

	//소켓 닫기
}
